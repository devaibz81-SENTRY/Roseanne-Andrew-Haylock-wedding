# Static only — no remotion, no scripts
remotion-wedding/
scripts/
index.literal-jungle.html
index.simple.html
index.thicket.html
thumbs/
reception.png

# Deps
node_modules/

# OS
.DS_Store
Thumbs.db
desktop.ini

# Large originals — keep clean/ but ignore raw duplicates if needed (currently tracked)
# Uncomment to ignore RAWs and only keep clean:
# photos/*.JPG
# photos/*.PNG
# photos/*.png
# photos/vlcsnap*.png
# photos/WhatsApp*.png

# Cache
*.log
.cache/
